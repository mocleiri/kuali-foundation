<?xml version="1.0" encoding="UTF-8"?>
<maven2-moduleset>
  <actions>
    <![CDATA[${jenkins.job.actions}]]>
  </actions>
  <description>
    <![CDATA[${jenkins.job.description}]]>
  </description>
  <logRotator>
    <daysToKeep>${jenkins.job.daysToKeep}</daysToKeep>
    <numToKeep>${jenkins.job.numToKeep}</numToKeep>
    <artifactDaysToKeep>${jenkins.job.artifactDaysToKeep}</artifactDaysToKeep>
    <artifactNumToKeep>${jenkins.job.artifactNumToKeep}</artifactNumToKeep>
  </logRotator>
  <keepDependencies>${jenkins.job.artifactNumToKeep}</keepDependencies>
  <properties />
  ${jenkins.job.scm.xml}
  <canRoam>${jenkins.job.artifactNumToKeep}</canRoam>
  <disabled>${jenkins.job.artifactNumToKeep}</disabled>
  <blockBuildWhenDownstreamBuilding>${jenkins.job.artifactNumToKeep}</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>${jenkins.job.artifactNumToKeep}</blockBuildWhenUpstreamBuilding>
  <triggers class="${jenkins.job.triggers.class}">
    <![CDATA[${jenkins.job.triggers}]]>
  </triggers>
  <concurrentBuild>${jenkins.job.artifactNumToKeep}</concurrentBuild>
  <rootModule>
    <groupId>${jenkins.project.groupId}</groupId>
    <artifactId>${jenkins.project.artifactId}</artifactId>
  </rootModule>
  <goals>${jenkins.mvn.goals}</goals>
  <aggregatorStyleBuild>${jenkins.job.aggregatorStyleBuild}</aggregatorStyleBuild>
  <incrementalBuild>${jenkins.job.incrementalBuild}</incrementalBuild>
  <usePrivateRepository>${jenkins.job.usePrivateRepository}</usePrivateRepository>
  <ignoreUpstremChanges>${jenkins.job.ignoreUpstremChanges}</ignoreUpstremChanges>
  <archivingDisabled>${jenkins.job.archivingDisabled}</archivingDisabled>
  <resolveDependencies>${jenkins.job.resolveDependencies}</resolveDependencies>
  <processPlugins>${jenkins.job.processPlugins}</processPlugins>
  <mavenValidationLevel>${jenkins.job.mavenValidationLevel}</mavenValidationLevel>
  <runHeadless>${jenkins.job.runHeadless}</runHeadless>
  <settingConfigId>${jenkins.job.settingConfigId}</settingConfigId>
  <globalSettingConfigId>${jenkins.job.globalSettingConfigId}</globalSettingConfigId>
  <reporters />
  <publishers />
  <buildWrappers />
  <prebuilders />
  <postbuilders />
  <runPostStepsIfResult>
    <name>FAILURE</name>
    <ordinal>2</ordinal>
    <color>RED</color>
  </runPostStepsIfResult>
</maven2-moduleset>