<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p" xmlns:util="http://www.springframework.org/schema/util" xmlns:context="http://www.springframework.org/schema/context" xsi:schemaLocation="http://www.springframework.org/schema/util 
        http://www.springframework.org/schema/util/spring-util.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

  <bean id="kuali.build.properties" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
    <property name="properties">
      <props>
        <prop key="kuali.home">${user.home}/.${kuali.groupId.code}</prop>
        <prop key="kuali.group.home">${kuali.home}/${project.groupId.code}</prop>
        <prop key="kuali.groupId">${kuali.groupId}</prop>
        <prop key="kuali.groupId.code">${kuali.groupId.code}</prop>
        <prop key="kuali.enc.mode">${kuali.enc.mode:NONE}</prop>
        <prop key="kuali.enc.password">${kuali.enc.password:NONE}</prop>
        <prop key="org.groupId">${kuali.groupId}</prop>
        <prop key="project.groupId">${project.groupId}</prop>
        <prop key="project.artifactId">${project.artifactId}</prop>
        <prop key="project.version">${project.version}</prop>
        <prop key="project.groupId.code">${project.groupId.code}</prop>
        <prop key="project.groupId.path">${project.groupId.path}</prop>
        <prop key="project.build.sourceEncoding">${project.build.sourceEncoding}</prop>
        <prop key="project.basedir">${project.basedir}</prop>
        <prop key="project.build.directory">${project.build.directory}</prop>
        <prop key="jdbc.driver.groupId">${jdbc.driver.groupId}</prop>
        <prop key="jdbc.driver.artifactId">${jdbc.driver.artifactId}</prop>
        <prop key="jdbc.driver.version">${jdbc.driver.version}</prop>
        <prop key="db.vendor">${db.vendor}</prop>
      </props>
    </property>
  </bean>

  <util:list id="locations" value-type="java.lang.String">
    <value>classpath:org/kuali/common/deploy/jdbc.properties</value>
    <value>classpath:${project.groupId.path}/${project.artifactId}.properties</value>
    <value>${kuali.group.home}/${project.artifactId}.properties</value>
    <value>classpath:org/kuali/common/deploy/${db.vendor}.properties</value>
    <value>classpath:org/kuali/common/sql/sql.properties</value>
    <value>classpath:org/kuali/common/sql/${db.vendor}.xml</value>
  </util:list>

  <import resource="classpath:org/kuali/common/deploy/spring/db-reset-context.xml" />

  <context:property-placeholder properties-ref="properties" system-properties-mode="${kuali.spring.systemPropertiesMode}" />

</beans>
