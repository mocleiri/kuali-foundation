schema.1=ole-impex-rice
schema.2=ole-impex-master

schema.loc.1=classpath:sql/${db.vendor}/${schema.1}.sql
schema.loc.2=classpath:sql/${db.vendor}/${schema.2}.sql

constraints.loc.1=classpath:sql/${db.vendor}/${schema.1}-constraints.sql
constraints.loc.2=classpath:sql/${db.vendor}/${schema.2}-constraints.sql

data.meta.1=classpath:META-INF/${db.vendor}/${schema.1}.tables
data.meta.2=classpath:META-INF/${db.vendor}/${schema.2}.tables
data.meta.3=classpath:META-INF/${db.vendor}/ole-liquibase-sql.resources