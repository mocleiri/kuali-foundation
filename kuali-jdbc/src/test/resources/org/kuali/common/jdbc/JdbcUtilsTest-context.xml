<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p" xmlns:util="http://www.springframework.org/schema/util" xmlns:context="http://www.springframework.org/schema/context" xsi:schemaLocation="http://www.springframework.org/schema/util 
        http://www.springframework.org/schema/util/spring-util.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

  <bean id="properties" class="org.kuali.common.util.PropertyFactoryBean" p:encoding="UTF-8" p:resolvePlaceholders="true">
    <property name="locations">
      <list>
        <value>classpath:jdbc.properties</value>
        <value>classpath:${db.vendor}.properties</value>
        <value>classpath:${db.vendor}.xml</value>
        <value>classpath:sql.properties</value>
        <value>classpath:impex.properties</value>
      </list>
    </property>
  </bean>

  <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <property name="properties" ref="properties" />
  </bean>

  <bean id="ds" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="driverClassName" value="${jdbc.driverClassName}" />
    <property name="url" value="${jdbc.url}" />
    <property name="username" value="${jdbc.username}" />
    <property name="password" value="${jdbc.password}" />
  </bean>

  <bean id="dbaDS" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="driverClassName" value="${jdbc.driverClassName}" />
    <property name="url" value="${jdbc.dba.url}" />
    <property name="username" value="${jdbc.dba.username}" />
    <property name="password" value="${jdbc.dba.password}" />
  </bean>

  <bean id="sqlReader" class="org.kuali.common.jdbc.DefaultSqlReader">
    <property name="delimiter" value="${sql.delimiter}" />
    <property name="trim" value="${sql.trim}" />
    <property name="commentToken" value="${sql.comment}" />
    <property name="lineSeparator" value="${sql.line.separator}" />
    <property name="encoding" value="${sql.encoding}" />
  </bean>

  <bean id="jdbcUtils" class="org.kuali.common.jdbc.JdbcUtils">
    <property name="dataSource" ref="ds" />
    <property name="sqlReader" ref="sqlReader" />
  </bean>

  <bean id="dba" class="org.kuali.common.jdbc.JdbcUtils">
    <property name="dataSource" ref="dbaDS" />
    <property name="sqlReader" ref="sqlReader" />
  </bean>

</beans>
