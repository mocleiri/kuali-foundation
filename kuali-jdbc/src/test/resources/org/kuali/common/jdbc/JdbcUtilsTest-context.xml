<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:util="http://www.springframework.org/schema/util" xmlns:context="http://www.springframework.org/schema/context" xsi:schemaLocation="http://www.springframework.org/schema/util 
        http://www.springframework.org/schema/util/spring-util.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

  <context:property-placeholder location="jdbc.properties,${db.vendor}.properties,${db.vendor}.xml" />

  <bean id="reset" class="org.kuali.common.jdbc.Reset">
    <property name="drop" value="${sql.${db.vendor}.drop}" />
    <property name="create" value="${sql.${db.vendor}.create}" />
    <property name="validate" value="${sql.${db.vendor}.validate}" />
  </bean>

  <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="driverClassName" value="${jdbc.driverClassName}" />
    <property name="url" value="${jdbc.dba.url}" />
    <property name="username" value="${jdbc.dba.username}" />
    <property name="password" value="${jdbc.dba.password}" />
  </bean>

  <bean id="sqlReader" class="org.kuali.common.jdbc.DefaultSqlReader">
    <property name="delimiter" value="${sql.delimiter}" />
  </bean>

  <bean id="jdbcUtils" class="org.kuali.common.jdbc.JdbcUtils">
    <property name="dataSource" ref="dataSource" />
    <property name="sqlReader" ref="sqlReader" />
  </bean>

</beans>