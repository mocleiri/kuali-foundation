# Override the dump dir so it goes to Maven's build directory
impex.working.dir=${project.build.directory}/impex

# This optional property makes it so that logging displays the name of the file where 
# schema.xml gets created relative to the Maven build directory
impex.dump.schema.dir.relative=${project.build.directory}
impex.staging.dir.relative=${project.build.directory}
