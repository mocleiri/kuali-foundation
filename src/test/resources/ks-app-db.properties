# Schema name
ks.schema=ks-app-db

# Resource locations
sql.schema=classpath:sql/${db.vendor}/${ks.schema}.sql
sql.data.list=classpath:META-INF/sql/${db.vendor}/${ks.schema}-data.resources
sql.constraints=classpath:sql/${db.vendor}/${ks.schema}-constraints.sql

# Controller properties
sql.schema.sequential=sql.schema
sql.data.concurrent=sql.data.list
sql.constraints.sequential=sql.constraints
