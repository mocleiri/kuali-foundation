<?xml version="1.0" encoding="UTF-8"?>
<!-- This is a template for the XML representation of a Jenkins job -->
<maven2-moduleset>
  <actions>${jenkins.job.actions}</actions>
  <description><![CDATA[${jenkins.job.description}]]></description>
  <logRotator>
    <daysToKeep>${jenkins.job.logRotator.daysToKeep}</daysToKeep>
    <numToKeep>${jenkins.job.logRotator.numToKeep}</numToKeep>
    <artifactDaysToKeep>${jenkins.job.logRotator.artifactDaysToKeep}</artifactDaysToKeep>
    <artifactNumToKeep>${jenkins.job.logRotator.artifactNumToKeep}</artifactNumToKeep>
  </logRotator>
  <keepDependencies>${jenkins.job.keepDependencies}</keepDependencies>
  <properties>${jenkins.job.properties}</properties>
  <scm class="${jenkins.job.scm.class}">${jenkins.job.scm}</scm>
  <canRoam>${jenkins.job.canRoam}</canRoam>
  <disabled>${jenkins.job.disabled}</disabled>
  <blockBuildWhenDownstreamBuilding>${jenkins.job.blockBuildWhenDownstreamBuilding}</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>${jenkins.job.blockBuildWhenUpstreamBuilding}</blockBuildWhenUpstreamBuilding>
  <triggers class="${jenkins.job.triggers.class}">${jenkins.job.triggers}</triggers>
  <concurrentBuild>${jenkins.job.concurrentBuild}</concurrentBuild>
  <rootModule>
    <groupId>${jenkins.job.rootModule.groupId}</groupId>
    <artifactId>${jenkins.job.rootModule.artifactId}</artifactId>
  </rootModule>
  <goals>${jenkins.job.goals}</goals>
  <aggregatorStyleBuild>${jenkins.job.aggregatorStyleBuild}</aggregatorStyleBuild>
  <incrementalBuild>${jenkins.job.incrementalBuild}</incrementalBuild>
  <usePrivateRepository>${jenkins.job.usePrivateRepository}</usePrivateRepository>
  <ignoreUpstremChanges>${jenkins.job.ignoreUpstremChanges}</ignoreUpstremChanges>
  <archivingDisabled>${jenkins.job.archivingDisabled}</archivingDisabled>
  <resolveDependencies>${jenkins.job.resolveDependencies}</resolveDependencies>
  <processPlugins>${jenkins.job.processPlugins}</processPlugins>
  <mavenValidationLevel>${jenkins.job.mavenValidationLevel}</mavenValidationLevel>
  <runHeadless>${jenkins.job.runHeadless}</runHeadless>
  <settingConfigId>${jenkins.job.settingConfigId}</settingConfigId>
  <globalSettingConfigId>${jenkins.job.globalSettingConfigId}</globalSettingConfigId>
  <reporters>${jenkins.job.reporters}</reporters>
  <publishers>${jenkins.job.publishers}</publishers>
  <buildWrappers>${jenkins.job.buildWrappers}</buildWrappers>
  <prebuilders>${jenkins.job.prebuilders}</prebuilders>
  <postbuilders>${jenkins.job.postbuilders}</postbuilders>
  <runPostStepsIfResult>${jenkins.job.runPostStepsIfResult}</runPostStepsIfResult>
</maven2-moduleset>