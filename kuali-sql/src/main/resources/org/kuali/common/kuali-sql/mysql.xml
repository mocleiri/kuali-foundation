<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright 2010-2014 The Kuali Foundation

    Licensed under the Educational Community License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.opensource.org/licenses/ecl2.php

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
  <comment>MySQL SQL</comment>
  <!-- Both the database and user must be gone when this SQL completes -->
  <entry key="mysql.drop">
    DROP DATABASE IF EXISTS ${sql.schema}
    /
    GRANT USAGE ON *.* TO '${sql.username}'@'%' IDENTIFIED BY '${sql.password}'
    /
    GRANT USAGE ON *.* TO '${sql.username}'@'localhost' IDENTIFIED BY '${sql.password}'
    /
    DROP USER '${sql.username}'@'%'
    /
    DROP USER '${sql.username}'@'localhost'
    /
  </entry>
  <entry key="mysql.create">
    CREATE DATABASE IF NOT EXISTS ${sql.schema} DEFAULT CHARACTER SET '${mysql.characterSet}' DEFAULT COLLATE '${mysql.collate}'
    /
    CREATE USER '${sql.username}'@'%' IDENTIFIED BY '${sql.password}'
    /
    CREATE USER '${sql.username}'@'localhost' IDENTIFIED BY '${sql.password}'
    /
    GRANT ALL ON ${sql.schema}.* TO '${sql.username}'@'%' WITH GRANT OPTION
    /
    GRANT ALL ON ${sql.schema}.* TO '${sql.username}'@'localhost' WITH GRANT OPTION
    /
  </entry>
  <entry key="mysql.characterSet">utf8</entry>
  <entry key="mysql.collate">utf8_bin</entry>
  <entry key="mysql.validate">
    SELECT 1
    /
  </entry>
  <!-- SQL to drop/create the 2 tables Liquibase needs in order to acquire a "lock" and go about its business -->
  <entry key="mysql.liquibase.drop">
    DROP TABLE IF EXISTS DATABASECHANGELOG
    /
    DROP TABLE IF EXISTS DATABASECHANGELOGLOCK
    /
  </entry>
  <entry key="mysql.liquibase.create">
    CREATE TABLE DATABASECHANGELOG (ID VARCHAR(63) NOT NULL
    , AUTHOR VARCHAR(63)
    , FILENAME VARCHAR(255)
    , DATEEXECUTED DATETIME NOT NULL
    , ORDEREXECUTED DECIMAL(22,0) NOT NULL
    , EXECTYPE VARCHAR(10) NOT NULL
    , MD5SUM VARCHAR(35)
    , DESCRIPTION VARCHAR(255)
    , COMMENTS VARCHAR(255)
    , TAG VARCHAR(255)
    , LIQUIBASE VARCHAR(20)
    , CONSTRAINT DATABASECHANGELOG_PK PRIMARY KEY(ID,AUTHOR,FILENAME)
    ) ENGINE InnoDB CHARACTER SET utf8 COLLATE utf8_bin
    /
    CREATE TABLE DATABASECHANGELOGLOCK (ID DECIMAL(22,0) NOT NULL
    , LOCKED DECIMAL(1,0) NOT NULL
    , LOCKGRANTED DATETIME
    , LOCKEDBY VARCHAR(255)
    , CONSTRAINT DATABASECHANGELOGLOCK_PK PRIMARY KEY(ID)
    ) ENGINE InnoDB CHARACTER SET utf8 COLLATE utf8_bin
    /
  </entry>
</properties>