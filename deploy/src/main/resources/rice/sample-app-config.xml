<config>
  <param name="appserver.url">${remote.public.url}</param>
  <!--
    <param name="app.context.name" />
    <param name="app.code" />
    <param name="application.url">${appserver.url}</param>
  -->
  <param name="environment" override="false">dev</param>
  <param name="externalizable.help.url">/kr/static/help/</param>
  <param name="externalizable.images.url">/kr/static/images/</param>
  <param name="kr.externalizable.images.url">${externalizable.images.url}</param>
  <param name="travel.externalizable.images.url">${externalizable.images.url}</param>
  <param name="datasource.url">${impex.url}</param>
  <param name="datasource.username">${impex.username}</param>
  <param name="datasource.password">${impex.password}</param>
  <param name="encryption.key">foobarbaz123456789</param>
  <param name="keystore.file">/usr/local/tomcat_deploy/kuali/main/dev/rice.keystore</param>
  <param name="keystore.alias">rice</param>
  <param name="keystore.password">r1c3pw</param>
</config>
