<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.kuali.rice.cas</groupId>
	<artifactId>kuali-cas</artifactId>
	<packaging>war</packaging>
	<version>1.0.0</version>
	<name>kuali-cas</name>
	<url>http://rice.kuali.org</url>

    <properties>
        <servlet-api.version>2.4</servlet-api.version>
        <jstl.version>1.1.2</jstl.version>
        <taglibs-standard.version>1.1.2</taglibs-standard.version>
        <jetty.version>6.1.7</jetty.version>
        <cas-server-core.version>3.3</cas-server-core.version>
        <cas-server-support-legacy.version>3.3</cas-server-support-legacy.version>
        <cas-webapp.version>3.3</cas-webapp.version>
        <cas-old.version>2.0.12</cas-old.version>      
        <cas-server-support-jdbc.version>3.3.1</cas-server-support-jdbc.version>      
	</properties>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-war-plugin</artifactId>
					<version>2.0.1</version>
					<configuration>						
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<dependencies>
					<dependency>
						<groupId>taglibs</groupId>
						<artifactId>standard</artifactId>
						<version>${taglibs-standard.version}</version>
					</dependency>
					<dependency>
						<groupId>javax.servlet</groupId>
						<artifactId>jstl</artifactId>
						<version>${jstl.version}</version>
					</dependency>
				</dependencies>
				<groupId>org.mortbay.jetty</groupId>
				<artifactId>maven-jetty-plugin</artifactId>
				<version>${jetty.version}</version>
				<configuration>
					<webAppConfig>
						<contextPath>/cas</contextPath>
					</webAppConfig>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>cas</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<configuration>
							<webXml>src/main/webapp/WEB-INF/web.xml</webXml>
						</configuration>
					</plugin>
				</plugins>
			</build>
			<dependencies>
				<dependency>
					<groupId>org.jasig.cas</groupId>
					<artifactId>cas-server-webapp</artifactId>
					<version>${cas-webapp.version}</version>
					<type>war</type>
				</dependency>
				<dependency>
					<groupId>org.jasig.cas</groupId>
					<artifactId>cas-server-core</artifactId>
					<version>${cas-server-core.version}</version>
				</dependency>
				<!--  
				<dependency>
				     <groupId>org.jasig.cas</groupId>
				     <artifactId>cas-server-support-legacy</artifactId>
				     <version>${cas-server-support-legacy.version}</version>
				</dependency>
				-->
				<dependency>
	                <groupId>javax.servlet</groupId>
	                <artifactId>servlet-api</artifactId>
	                <version>${servlet-api.version}</version>
	                <scope>provided</scope>
	            </dependency>
	            <dependency>
	                <groupId>javax.servlet</groupId>
	                <artifactId>jstl</artifactId>
	                <version>${jstl.version}</version>
	                <scope>provided</scope>
	            </dependency>
	            <!--  
				<dependency>
					<groupId>cas</groupId>
					<artifactId>cas</artifactId>
					<version>${cas-old.version}</version>
				</dependency>
				-->
				<dependency>
				    <groupId>org.jasig.cas</groupId>
				    <artifactId>cas-server-support-jdbc</artifactId>
				    <version>${cas-server-support-jdbc.version}</version>
				</dependency>
				<dependency>
				    <groupId>commons-dbcp</groupId>
				    <artifactId>commons-dbcp</artifactId>
				    <version>1.2.1</version>
				    <scope>runtime</scope>
				</dependency>
				<dependency>
				    <groupId>ojdbc</groupId>
				    <artifactId>ojdbc</artifactId>
  				    <version>14</version>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
		
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>cas</groupId>
				<artifactId>cas</artifactId>
				<version>${cas-old.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>	
		
	<repositories>
		<repository>
			<id>jasig</id>
			<name>JA-SIG Repository</name>
			<url>http://developer.ja-sig.org/maven2</url>
		</repository>
        <repository>
            <id>kuali</id>
            <name>Kuali Repository</name>
            <url>https://test.kuali.org/maven</url>
        </repository>
   	</repositories>
</project>
